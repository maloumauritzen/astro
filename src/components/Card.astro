---
const { image, navn, rolle } = Astro.props;
---

<article class="card">
  <img src={image} alt="{navn}" />
  <h3>{navn}</h3>
  <p>{rolle}</p>
</article>

<style>
  .card img {
    border-radius: 50%;
    width: 150px;
    height: 150px;
    object-fit: cover;
  }
</style>
